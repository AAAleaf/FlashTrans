# ── Python 虚拟环境 / 缓存（CT2 桥接开发用）──
.venv/
__pycache__/
*.pyc
*.pyd
*.pyo
.python-version
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ── 构建产物 ──
build/
dist/
target/
Output/
*.zip
*.exe
*.msi
*.7z
*.rar

# ── 打包脚本本地产物（Inno Setup / PyInstaller）──
*.iss
*.spec
!bridge.spec

# ── 模型与缓存（体积大，经网盘分发，不入库）──
models/
.cache/
.tmp/

# ── 本地私有内容（内部笔记 / 设计稿 / agent 配置，含本机路径）──
.claude/
design/

# ── 日志 / 崩溃转储（可能含本机路径）──
*.log
*.tmp
*.bak
*.old
*.dmp
*.dump
*.stackdump
*.minidump
nuitka-crash-report.xml

# ── 环境变量 / 本地配置（可能含个人信息）──
.env
.env.*
!.env.example
*.reg
*.ini
*.toml.local
*.yaml.local
*.yml.local
*.json.local

# ── 密钥 / 证书 ──
*.pem
*.key
*.pfx
*.p12
*.jks
*.kdbx
*.crt
*.cer

# ── 应用数据 ──
*.sqlite
*.db
*.sqlite3
*.db3

# ── 系统 / IDE ──
.DS_Store
._*
Thumbs.db
Desktop.ini
ehthumbs.db
ehthumbs_vista.db
*.swp
*~
.fuse_hidden*
.idea/
.vscode/
.vs/
*.suo
*.user
*.userosscache
*.sln.docstates
